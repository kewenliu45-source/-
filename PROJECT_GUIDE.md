# 项目总体说明书

## 项目目标

智能库存预警系统是一个面向零售/分销企业的库存风险管理工具。系统通过分析本地库存、近期销售、总部可用量三类数据，自动识别缺货风险并生成调货建议，帮助企业降低断货损失、优化库存周转。

## 项目业务背景

### 业务痛点
- 门店/分仓库存不足时，人工判断调货时机容易滞后
- 总部与门店之间的库存信息不对称
- 缺乏量化的库存风险评估和调货建议

### 解决方案
- 自动计算每 SKU 的可售天数
- 根据安全库存天数（7天）判断是否需要调货
- 综合考虑总部可用库存，生成具体调货建议

## 当前开发阶段

**阶段：V2 - 多数据源对接**

已完成：
- ✅ Excel 三表上传分析（自动检测表头位置）
- ✅ 数据库直连分析（MySQL/PostgreSQL/SQL Server/Oracle）
- ✅ 畅捷通 T+ OpenAPI 对接（以销售为主匹配库存）
- ✅ OAuth 授权流程
- ✅ 企业微信通知框架
- ✅ 导出功能（销售表和库存表 Excel 导出）
- ✅ 总部库存表存货编码匹配（取后11位）
- ✅ 仓库编码格式统一（如 6.0 → 006）

进行中：
- 🔄 T+ 销售数据实时查询优化
- 🔄 预警消息自动推送

## 已实现功能

| 功能模块 | 状态 | 说明 |
|---------|------|------|
| Excel 上传分析 | ✅ 完成 | 支持库存表、销售表、总部库存表三表合并，自动检测表头位置 |
| 数据库分析 | ✅ 完成 | 支持多种数据库，含 mock 模式测试，可选上传总部库存表 |
| T+ OpenAPI | ✅ 完成 | 对接畅捷通 T+ 获取实时库存和销售数据，以销售为主匹配库存 |
| 预警计算 | ✅ 完成 | 红色/黄色预警、可售天数、调货建议 |
| 导出功能 | ✅ 完成 | 支持导出近7天销售表和库存表（Excel 格式） |
| 企业微信通知 | ✅ 框架 | 支持 Webhook 发送，待配置 |
| OAuth 授权 | ✅ 完成 | T+ OAuth2.0 授权码模式 |

## 未实现功能

| 功能模块 | 优先级 | 说明 |
|---------|--------|------|
| 定时任务 | 中 | 每日自动执行预警分析 |
| 历史数据对比 | 低 | 库存趋势分析 |
| 多仓库汇总 | 低 | 跨仓库库存合并分析 |
| 用户权限 | 低 | 多用户登录管理 |

## 核心业务流程

```
数据输入（三选一）
    ├── Excel 上传（库存表 + 销售表 + 总部库存表，自动检测表头）
    ├── 数据库直连（SQL 查询，可选上传总部库存表）
    └── T+ OpenAPI（实时接口）
          ↓
    数据标准化（以销售为主）
    ├── 销售数据作为主体（LEFT JOIN 库存）
    ├── 统一字段：存货编码、尺码、库存量、销量
    ├── 仓库编码格式统一（如 6.0 → 006）
    └── 总部库存编码取后11位匹配
          ↓
    预警计算
    ├── 可售天数 = 当前可用量 / 日均销量
    ├── 目标库存 = 日均销量 × 7天
    └── 建议调货量 = 目标库存 - 当前可用量
          ↓
    预警状态判定
    ├── 红色预警：当前可用量 ≤ 0
    ├── 黄色预警：可售天数 < 7天
    └── 正常：可售天数 ≥ 7天
          ↓
    结果输出
    ├── Web 页面展示（预警明细 + 调货建议）
    ├── 导出 Excel（/export/sales 和 /export/inventory）
    └── 企业微信通知（可选）
```

## 关键设计思想

### 1. 标准数据层
所有数据源（Excel、数据库、T+ API）最终都转换为统一的标准 DataFrame，字段包括：
- 存货编码、存货、尺码
- 近7天销量、日均销量
- 仓库编码、仓库
- 当前现存量、当前可用量
- 总部库存

### 2. 数据源抽象
通过 `data_sources` 目录实现数据源解耦：
- `base.py`：定义标准字段和校验
- `excel_source.py`：Excel 数据处理
- `database_source.py`：数据库查询
- `tplus_openapi_source.py`：T+ API 对接

### 3. 预警引擎独立
`warning_service.py` 只负责计算，不关心数据来源，便于单元测试和规则调整。

### 4. 配置外部化
所有敏感信息和环境相关配置通过 `.env` 文件管理，代码中不硬编码。

### 5. 仓库维度筛选
默认只分析指定仓库（`WARNING_WAREHOUSE_CODE=006`），避免全仓库数据干扰预警结果。仓库编码格式统一处理（如 Excel 中的浮点数 6.0 转换为字符串 006）。

### 6. 以销售为主的数据合并
所有数据源（Excel、数据库、T+ API）均以销售数据为主体，通过 LEFT JOIN 匹配库存数据，确保有销售记录但无库存的 SKU 也能被识别为预警项。

### 7. 自动检测表头
Excel 销售表和库存表支持自动检测表头位置，扫描前10行查找包含"存货编码"的行作为表头，兼容不同来源的 Excel 格式。

### 8. 总部库存编码适配
总部库存表的存货编码可能包含前缀，系统自动取后11位与销售表/库存表进行匹配。
